# React + TypeScript + Vite + Konva

A demo project implementing Dijkstra's shortest path algorithm with a binary heap as a priority queue. Even though it might not be obvious from the visualization alone, this is a weighted graph (the edges have random weights). This is why the shortest path might not be visually shortest.
